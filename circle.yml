machine:
  services:
    - docker

dependencies:
  override:
    - sudo pip install --upgrade docker-compose
    - npm install

test:
  override:
    - docker-compose run -d --no-deps api
    - npm run lint
    - npm test
